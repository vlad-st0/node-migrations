Migration system i made for fun, inspired by diesel (rust orm) and eloquent (laravel orm) migrations. 
It supports mysql2 and pg database drivers.

Available Makefile and npm commands:
- migrate 
- rollback
- refresh
- seed